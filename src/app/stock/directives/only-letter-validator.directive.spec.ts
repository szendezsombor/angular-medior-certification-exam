import { OnlyLetterValidatorDirective } from './only-letter-validator.directive';

describe('OnlyLetterValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyLetterValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
